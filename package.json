{
  "name": "generative-views",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "preinstall": "npx only-allow pnpm",
    "app": "pnpm --filter next-app dev",
    "toolbox": "pnpm --filter @arklo/toolbox tsc --watch",
    "dev": "npm-run-all --parallel app toolbox"
  },
  "devDependencies": {
    "prettier": "^2.8.8"
  },
  "dependencies": {
    "npm-run-all": "^4.1.5"
  }
}
